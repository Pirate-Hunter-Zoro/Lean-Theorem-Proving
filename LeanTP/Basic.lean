import Mathlib

/-!
# LeanTP.Basic

Shared definitions and helper lemmas. This starts empty on purpose: things earn
their way in here only after the same idea has been needed in two different
exercises. Premature abstraction is how a learning repo turns into a library
nobody understands.

The rule mirrors `helpermath` in Algo-Solutions — a helper lives here when it is
genuinely reusable, not when it is merely convenient once.
-/

namespace LeanTP

end LeanTP
